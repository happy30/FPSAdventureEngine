using System.Collections;
using System.Collections.Generic;
using EventObjects;
using UnityEngine;
using UnityEngine.UI;

public class InspectModeBackgroundEnabler : MonoBehaviour
{
   // public InspectModeComponentWithEvent Condition;
    //public InspectModeInteractionPointWithEvent Condition2;
    public Image Component;

    // Update is called once per frame
    void Update()
    {
        //Component.enabled = Condition.Value != null || Condition2.Value != null;
    }
}
